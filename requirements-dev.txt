# Build Packages
build
setuptools
wheel

# Lint & Coverage
ruff
# mypy 2.x pulls in ast-serialize, whose PyO3 build does not support CPython 3.13t.
mypy<2
coverage

# Test
pytest
coredumpy

# Devtools
pystack >= 1.6.0; sys_platform == 'linux'
psutil

# 3rd party packages for test
ipywidgets>8.0.5
loky>3.5.0
jaxlib; sys_platform != 'win32' and python_version < '3.11'
